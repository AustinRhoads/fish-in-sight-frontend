

export default function userReducer (state= [], action){

    switch(action.type){

        case "LOGIN":
            //fetch request maybe
        return state;


        default:
            return state;
    }
}